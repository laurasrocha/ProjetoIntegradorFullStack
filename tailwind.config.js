/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // ativa modo escuro controlado por classe
    content: ['./src/**/*.{js,jsx,ts,tsx,html}'], // ajuste conforme sua estrutura
    theme: {
        extend: {},
    },
    plugins: [],
}
